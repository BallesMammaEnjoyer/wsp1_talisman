{% extends "layout.njk" %}

{% block content %}
<main class="wrapper">
    <h1>
        {{title}}
    </h1>
    <p>
        Information om {{artist.name}}.
    </p>
    {% if artist.image %}
    <img src="/img/{{artist.image}}" alt="Bild på {{artist.name}}">
   {% endif %}
    </main>

{% endblock %}      