{% extends "layout.njk" %}

{% block content %}

<body>
    <header>
        <div class="container header">

            <h1>
                {{title}}
            </h1>
            <a href="artists" class="btn btn-rounded">Return</a>
        </div>
    </header>

</body>
<main class="wrapper">
    <div class="container">
        {% for album in albums %}
        <h1>HEJEHEJ</h1>
        {% endfor %}
        <p>
            Information om {{artist.name}}.
        </p>

        {% if artist.image %}
        <img src="/img/{{artist.image}}" alt="Bild på {{artist.name}}">
        {% endif %}
    </div>
</main>

{% endblock %}